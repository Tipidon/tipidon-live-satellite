# TIPIDON LIVE v4 — MAP ENGINE FIX

MAP = OpenStreetMap roads/labels. SATELLITE = Esri World Imagery. HYBRID = imagery + roads/place labels. STREET = public KartaView imagery. NASA layers are optional Earth-observation overlays.

Replace index.html, style.css, app.js and README.md in the existing GitHub repository, then commit.

Satellite imagery is not a continuous live camera feed; observation/update times depend on the source. Review current provider terms before commercial launch.
